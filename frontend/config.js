// Default/local fallback — overwritten by build.js during the Vercel build
// using the API_BASE_URL environment variable. Safe to edit for local dev.
window.HABB_CONFIG = {
  API_BASE_URL: 'http://localhost:8000/api/v1'
};
